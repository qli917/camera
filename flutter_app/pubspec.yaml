name: timestamp_zip_ui
description: Drag-drop video and zip UI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.2
  dio: ^5.7.0

flutter:
  uses-material-design: true
